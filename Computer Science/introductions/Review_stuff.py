# particle moving from the bottom to the top of a window

import pygame
import pygame_helper

pygame.init()

# Set the dimensions of the window
width = 600
height = 1200

# create a program window
# win is a refrence to the window object created by pygame
win = pygame.display.set_mode((width, height))

